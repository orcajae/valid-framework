# Core dependencies (mirrors [project.dependencies] in pyproject.toml).
# For extras use: pip install -e ".[ml,data,dev]"
#   ml   — catboost, lightgbm, torch (reference-experiment models)
#   data — ccxt (public OHLCV download for `make reproduce-real`)
#   dev  — pytest
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
